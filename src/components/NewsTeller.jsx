import React from 'react';

const NewsTeller = ({ news }) => {
    return (
        <div>
            <h2>Latest News</h2>
            {/* <ul>
                {news.map((item, index) => (
                    <li key={index}>{item}</li>
                ))}
            </ul> */}
        </div>
    );
};

export default NewsTeller;

